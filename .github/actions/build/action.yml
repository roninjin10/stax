name: Build
runs:
  using: composite
  steps:
    - name: 'Setup'
      uses: ./.github/actions/setup

    - name: Run build
      run: pnpm build:ci
      shell: bash
      env:
        NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}

